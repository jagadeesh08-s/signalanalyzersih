{
  "name": "sih26147-signal-analyzer",
  "version": "1.0.0",
  "description": "Root package for running frontend and backend concurrently",
  "scripts": {
    "install:all": "cd frontend && npm install",
    "full:dev": "npx concurrently \"cd frontend && npm run dev\" \"cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000\""
  }
}
